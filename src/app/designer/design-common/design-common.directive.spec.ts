import {DesignCommonDirective} from "./design-common.directive";

describe('DesignCommonDirective', () => {
  it('should create an instance', () => {
    const directive = new DesignCommonDirective();
    expect(directive).toBeTruthy();
  });
});
