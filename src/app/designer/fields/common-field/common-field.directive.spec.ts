import {CommonFieldDirective} from "./common-field.directive";

describe('CommonFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new CommonFieldDirective();
    expect(directive).toBeTruthy();
  });
});
